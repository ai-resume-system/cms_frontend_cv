export enum EUserRole {
  Admin = "admin",
  JobSeeker = "job_seeker",
  Recruiter = "recruiter",
}

export enum EUserStatus {
  Active = "active",
  Unverified = "unverified",
  Locked = "locked",
}
